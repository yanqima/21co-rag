# Development and testing dependencies
# Install with: pip install -r requirements-dev.txt

# Testing frameworks
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2  # For testing HTTP endpoints
faker==20.1.0  # For generating test data

# Code quality and formatting
black==23.12.1
isort==5.13.2
mypy==1.7.1
pre-commit==3.6.0

# Monitoring and observability (for development)
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
opentelemetry-instrumentation-fastapi==0.42b0

# Optional dependencies for development
tiktoken==0.5.2  # Token counting (if needed for development)
celery==5.3.4   # Task queue (if implementing async processing)
